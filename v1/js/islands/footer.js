// Footer Island — enhances the existing <footer> markup.
// No-op for now. The footer is fully static HTML.
// This island exists as a placeholder for future dynamic behavior
// (e.g. live year update, dynamic taglines, etc.)

export default function footer(el) {
  // Static content — nothing to hydrate.
  // The HTML in the page is the source of truth.
}
